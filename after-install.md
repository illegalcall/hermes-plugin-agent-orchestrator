## Agent Orchestrator plugin installed

Add `AO_CWD` to `~/.hermes/.env`:

```
AO_CWD=/path/to/your/agent-orchestrator
```

Restart the gateway, then try: **"what's happening?"**

17 tools available — say **"help with AO"** for the full list.
